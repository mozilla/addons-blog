<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} - {% endif %}{{ markup.siteTitle }}</title>

    {{ blogpost.seoHead | safe({ isHeadMarkup: true }) }}

    <link rel="apple-touch-icon" href="{{ config.asset_path|safe }}/assets/img/favicon.png">
    <link rel="shortcut icon" href="{{ config.asset_path|safe }}/assets/img/favicon.ico">

    {%- if not config.no_atom_feed %}
    <link rel="alternate" type="application/atom+xml" title="{{ markup.siteTitle }} RSS Feed"" href="/feed.xml">
    {%- endif %}

    {% block stylesheets %}
    <link rel="stylesheet" href="{{ config.asset_path|safe }}/assets/css/styles.css">
    {%- endblock %}
  </head>
  <body>
    <header
      class="Header"
      style="background: #20123a url('{{ featuredImage }}') no-repeat center;">
      <a class="Header-title" href="/">
        <span class="visually-hidden">Firefox Browser Add-ons</span>
      </a>

      <h1>{% if title %}{{ title }}{% else %}{{ markup.siteTitle }}{% endif %}</h1>
    </header>

    {% block content %}
    <main aria-label="Content">
      {{ content | safe }}
    </main>
    {% endblock content %}

    {{ markup.footer | safe }}

    {% block javascripts %}
    <script src="{{ config.asset_path|safe }}/assets/js/bundle.js"></script>
    {% endblock %}
  </body>
</html>
